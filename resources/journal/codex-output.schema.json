{
  "type": "object",
  "properties": {
    "summary": {"type": "string"},
    "edits": {
      "type": "array",
      "minItems": 0,
      "maxItems": 100,
      "items": {
        "type": "object",
        "properties": {
          "action": {"type": "string", "enum": ["patch", "insert", "create"]},
          "target": {"type": ["string", "null"]},
          "kind": {"type": "string", "enum": ["prose", "code", "namespace"]},
          "before": {"type": ["string", "null"]},
          "after": {"type": "string"}
        },
        "required": ["action", "target", "kind", "before", "after"],
        "additionalProperties": false
      }
    }
  },
  "required": ["summary", "edits"],
  "additionalProperties": false
}
